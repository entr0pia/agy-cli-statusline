[project]
name = "agy-cli-statusline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "entr0pia", email = "entr0pia" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
agy-cli-statusline = "agy_cli_statusline:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
